<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.soulone.stwp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MisRutinasActivity"></activity>
        <activity
            android:name=".RutinasActivity_beta"
            android:label="@string/title_activity_rutinas_beta"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RutinasActivity"
            android:label="@string/title_activity_rutinas" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SingUpActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>